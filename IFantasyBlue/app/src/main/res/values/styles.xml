<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" >
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="RankDialog">
        <item name="android:windowEnterAnimation">@anim/rank_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/rank_exit_anim</item>
    </style>

</resources>
